export enum WaiverType {
  PERMANENT = 'permanent',
  TEMPORARY = 'temporary',
}

export enum WaiverStatus {
  PENDING = 'Pending',
  APPROVED = 'Approved',
  REJECTED = 'Rejected',
  CANCELED = 'Canceled',
  EXPIRED = 'Expired',
}
